const Product = require("./product");
const User = require("./user");
const Cart = require("./cart");
const Order = require("./order");

module.exports = { Product, User, Cart, Order };
// module.exports = { Product, User, Cart, CartItem, Order, OrderItem };
